import 'package:flutter/material.dart';

import 'src/contactos.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Contactos();
  }
}
