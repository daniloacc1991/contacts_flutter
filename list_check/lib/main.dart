import 'package:flutter/material.dart';
import 'package:list_check/src/list-switch.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListSwitch();
  }
}
